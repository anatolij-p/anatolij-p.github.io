# Redesign Anime.js Preview

Saved preview for the `redesign-animejs` branch.

- `index.html` — current approved animated landing-page preview.
- `design-directions.html` — earlier direction selection mockup.

Open from the project root with a static server, for example:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000/docs/previews/redesign-animejs/`.
